{
  "name": "@coopsource/web",
  "version": "0.1.0",
  "private": true,
  "description": "Co-op Source Network — SvelteKit frontend",
  "type": "module",
  "scripts": {
    "dev": "vite dev",
    "build": "vite build",
    "preview": "vite preview",
    "prepare": "svelte-kit sync",
    "check": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json",
    "test": "vitest run",
    "test:e2e": "playwright test",
    "test:e2e:real": "playwright test --project=real",
    "test:e2e:mocked": "playwright test --project=mocked",
    "test:e2e:ui": "playwright test --ui",
    "clean": "rm -rf .svelte-kit dist"
  },
  "dependencies": {
    "@coopsource/common": "workspace:*",
    "@lucide/svelte": "0.575.0"
  },
  "devDependencies": {
    "@playwright/test": "^1",
    "@sveltejs/adapter-auto": "^4",
    "@sveltejs/kit": "^2.20",
    "@sveltejs/vite-plugin-svelte": "^5",
    "@tailwindcss/vite": "^4",
    "@types/pg": "^8",
    "kysely": "^0.27",
    "pg": "^8",
    "svelte": "^5.19",
    "svelte-check": "^4",
    "tailwindcss": "^4",
    "typescript": "^5.7",
    "vite": "^6",
    "vitest": "^3"
  }
}
